/*
 * Proxmox Virtual Environment API
 *
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PveVirtiofsConfig {

    /// The caching policy the file system should use (auto, always, metadata, never).
    #[serde(rename = "cache", skip_serializing_if = "Option::is_none")]
    pub cache: Option<models::PveQemuCacheEnum>,

    /// Honor the O_DIRECT flag passed down by guest applications.
    #[serde(rename = "direct-io", skip_serializing_if = "Option::is_none")]
    pub direct_io: Option<models::PveBoolean>,

    /// Mapping identifier of the directory mapping to be shared with the guest. Also used as a mount tag inside the VM.
    #[serde(rename = "dirid")]
    pub dirid: String,

    /// Enable support for POSIX ACLs (enabled ACL implies xattr) for this mount.
    #[serde(rename = "expose-acl", skip_serializing_if = "Option::is_none")]
    pub expose_acl: Option<models::PveBoolean>,

    /// Enable support for extended attributes for this mount.
    #[serde(rename = "expose-xattr", skip_serializing_if = "Option::is_none")]
    pub expose_xattr: Option<models::PveBoolean>,


}

impl PveVirtiofsConfig {
    pub fn new(dirid: String) -> PveVirtiofsConfig {
        PveVirtiofsConfig {
            
            cache: None,
            
            direct_io: None,
            
            dirid,
            
            expose_acl: None,
            
            expose_xattr: None,
            
        }
    }
}


impl PveVirtiofsConfig {
    /// Serialise this PveVirtiofsConfig into Proxmox's CLI-style shorthand
    /// string (`key=value,…`). The property marked `x-pve-default-key`
    /// is emitted positionally without a `key=` prefix; aliases collapse
    /// multiple property names to the same wire key.
    ///
    /// Example: `PveVirtiofsConfig `
    /// → `"virtio,bridge=vmbr0"`
    pub fn to_shorthand(&self) -> String {
        let mut parts: Vec<String> = Vec::new();
        
        
        parts.push(format!("{}", self.dirid));
        
        
        
        
        if let Some(ref v) = self.cache {
            parts.push(format!("cache={}", v));
        }
        
        
        if let Some(ref v) = self.direct_io {
            parts.push(format!("direct-io={}", v));
        }
        
        
        if let Some(ref v) = self.expose_acl {
            parts.push(format!("expose-acl={}", v));
        }
        
        
        if let Some(ref v) = self.expose_xattr {
            parts.push(format!("expose-xattr={}", v));
        }
        
        parts.join(",")
    }
}

