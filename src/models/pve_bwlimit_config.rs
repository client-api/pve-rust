/*
 * Proxmox Virtual Environment API
 *
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PveBwlimitConfig {

    /// bandwidth limit in KiB/s for cloning disks
    #[serde(rename = "clone", skip_serializing_if = "Option::is_none")]
    pub clone: Option<f64>,

    /// default bandwidth limit in KiB/s
    #[serde(rename = "default", skip_serializing_if = "Option::is_none")]
    pub default: Option<f64>,

    /// bandwidth limit in KiB/s for migrating guests (including moving local disks)
    #[serde(rename = "migration", skip_serializing_if = "Option::is_none")]
    pub migration: Option<f64>,

    /// bandwidth limit in KiB/s for moving disks
    #[serde(rename = "move", skip_serializing_if = "Option::is_none")]
    pub r#move: Option<f64>,

    /// bandwidth limit in KiB/s for restoring guests from backups
    #[serde(rename = "restore", skip_serializing_if = "Option::is_none")]
    pub restore: Option<f64>,


}

impl PveBwlimitConfig {
    pub fn new() -> PveBwlimitConfig {
        PveBwlimitConfig {
            
            clone: None,
            
            default: None,
            
            migration: None,
            
            r#move: None,
            
            restore: None,
            
        }
    }
}


impl PveBwlimitConfig {
    /// Serialise this PveBwlimitConfig into Proxmox's CLI-style shorthand
    /// string (`key=value,…`). The property marked `x-pve-default-key`
    /// is emitted positionally without a `key=` prefix; aliases collapse
    /// multiple property names to the same wire key.
    ///
    /// Example: `PveBwlimitConfig `
    /// → `"virtio,bridge=vmbr0"`
    pub fn to_shorthand(&self) -> String {
        let mut parts: Vec<String> = Vec::new();
        
        
        
        if let Some(ref v) = self.clone {
            parts.push(format!("clone={}", v));
        }
        
        
        if let Some(ref v) = self.default {
            parts.push(format!("default={}", v));
        }
        
        
        if let Some(ref v) = self.migration {
            parts.push(format!("migration={}", v));
        }
        
        
        if let Some(ref v) = self.r#move {
            parts.push(format!("move={}", v));
        }
        
        
        if let Some(ref v) = self.restore {
            parts.push(format!("restore={}", v));
        }
        
        parts.join(",")
    }
}

