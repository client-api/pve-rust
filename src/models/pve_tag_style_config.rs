/*
 * Proxmox Virtual Environment API
 *
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PveTagStyleConfig {

    /// Controls if filtering for unique tags on update should check case-sensitive.
    #[serde(rename = "case-sensitive", skip_serializing_if = "Option::is_none")]
    pub case_sensitive: Option<models::PveBoolean>,

    /// Manual color mapping for tags (semicolon separated).
    #[serde(rename = "color-map", skip_serializing_if = "Option::is_none")]
    pub color_map: Option<String>,

    /// Controls the sorting of the tags in the web-interface and the API update.
    #[serde(rename = "ordering", skip_serializing_if = "Option::is_none")]
    pub ordering: Option<models::PveClusterOptionsOrderingEnum>,

    /// Tag shape for the web ui tree. 'full' draws the full tag. 'circle' draws only a circle with the background color. 'dense' only draws a small rectancle (useful when many tags are assigned to each guest).'none' disables showing the tags.
    #[serde(rename = "shape", skip_serializing_if = "Option::is_none")]
    pub shape: Option<models::PveClusterOptionsShapeEnum>,


}

impl PveTagStyleConfig {
    pub fn new() -> PveTagStyleConfig {
        PveTagStyleConfig {
            
            case_sensitive: None,
            
            color_map: None,
            
            ordering: None,
            
            shape: None,
            
        }
    }
}


impl PveTagStyleConfig {
    /// Serialise this PveTagStyleConfig into Proxmox's CLI-style shorthand
    /// string (`key=value,…`). The property marked `x-pve-default-key`
    /// is emitted positionally without a `key=` prefix; aliases collapse
    /// multiple property names to the same wire key.
    ///
    /// Example: `PveTagStyleConfig `
    /// → `"virtio,bridge=vmbr0"`
    pub fn to_shorthand(&self) -> String {
        let mut parts: Vec<String> = Vec::new();
        
        
        
        if let Some(ref v) = self.case_sensitive {
            parts.push(format!("case-sensitive={}", v));
        }
        
        
        if let Some(ref v) = self.color_map {
            parts.push(format!("color-map={}", v));
        }
        
        
        if let Some(ref v) = self.ordering {
            parts.push(format!("ordering={}", v));
        }
        
        
        if let Some(ref v) = self.shape {
            parts.push(format!("shape={}", v));
        }
        
        parts.join(",")
    }
}

