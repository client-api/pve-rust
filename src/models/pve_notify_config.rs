/*
 * Proxmox Virtual Environment API
 *
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 9.x
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PveNotifyConfig {

    /// UNUSED - Use datacenter notification settings instead.
    #[serde(rename = "fencing", skip_serializing_if = "Option::is_none")]
    pub fencing: Option<models::PveFencingEnum>,

    /// DEPRECATED: Use datacenter notification settings instead. Control how often the daily update job should send out notifications: * 'auto' daily for systems with a valid subscription, as those are assumed to be  production-ready and thus should know about pending updates. * 'always' every update, if there are new pending updates. * 'never' never send a notification for new pending updates. 
    #[serde(rename = "package-updates", skip_serializing_if = "Option::is_none")]
    pub package_updates: Option<models::PveClusterOptionsPackageUpdatesEnum>,

    /// UNUSED - Use datacenter notification settings instead.
    #[serde(rename = "replication", skip_serializing_if = "Option::is_none")]
    pub replication: Option<models::PveFencingEnum>,

    /// UNUSED - Use datacenter notification settings instead.
    #[serde(rename = "target-fencing", skip_serializing_if = "Option::is_none")]
    pub target_fencing: Option<String>,

    /// UNUSED - Use datacenter notification settings instead.
    #[serde(rename = "target-package-updates", skip_serializing_if = "Option::is_none")]
    pub target_package_updates: Option<String>,

    /// UNUSED - Use datacenter notification settings instead.
    #[serde(rename = "target-replication", skip_serializing_if = "Option::is_none")]
    pub target_replication: Option<String>,


}

impl PveNotifyConfig {
    pub fn new() -> PveNotifyConfig {
        PveNotifyConfig {
            
            fencing: None,
            
            package_updates: None,
            
            replication: None,
            
            target_fencing: None,
            
            target_package_updates: None,
            
            target_replication: None,
            
        }
    }
}


impl PveNotifyConfig {
    /// Serialise this PveNotifyConfig into Proxmox's CLI-style shorthand
    /// string (`key=value,…`). The property marked `x-pve-default-key`
    /// is emitted positionally without a `key=` prefix; aliases collapse
    /// multiple property names to the same wire key.
    ///
    /// Example: `PveNotifyConfig `
    /// → `"virtio,bridge=vmbr0"`
    pub fn to_shorthand(&self) -> String {
        let mut parts: Vec<String> = Vec::new();
        
        
        
        if let Some(ref v) = self.fencing {
            parts.push(format!("fencing={}", v));
        }
        
        
        if let Some(ref v) = self.package_updates {
            parts.push(format!("package-updates={}", v));
        }
        
        
        if let Some(ref v) = self.replication {
            parts.push(format!("replication={}", v));
        }
        
        
        if let Some(ref v) = self.target_fencing {
            parts.push(format!("target-fencing={}", v));
        }
        
        
        if let Some(ref v) = self.target_package_updates {
            parts.push(format!("target-package-updates={}", v));
        }
        
        
        if let Some(ref v) = self.target_replication {
            parts.push(format!("target-replication={}", v));
        }
        
        parts.join(",")
    }
}

