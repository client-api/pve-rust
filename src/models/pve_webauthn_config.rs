/*
 * Proxmox Virtual Environment API
 *
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PveWebauthnConfig {

    /// Whether to allow the origin to be a subdomain, rather than the exact URL.
    #[serde(rename = "allow-subdomains", skip_serializing_if = "Option::is_none")]
    pub allow_subdomains: Option<models::PveBoolean>,

    /// Relying party ID. Must be the domain name without protocol, port or location. Changing this *will* break existing credentials.
    #[serde(rename = "id", skip_serializing_if = "Option::is_none")]
    pub id: Option<String>,

    /// Site origin. Must be a `https://` URL (or `http://localhost`). Should contain the address users type in their browsers to access the web interface. Changing this *may* break existing credentials.
    #[serde(rename = "origin", skip_serializing_if = "Option::is_none")]
    pub origin: Option<String>,

    /// Relying party name. Any text identifier. Changing this *may* break existing credentials.
    #[serde(rename = "rp", skip_serializing_if = "Option::is_none")]
    pub rp: Option<String>,


}

impl PveWebauthnConfig {
    pub fn new() -> PveWebauthnConfig {
        PveWebauthnConfig {
            
            allow_subdomains: None,
            
            id: None,
            
            origin: None,
            
            rp: None,
            
        }
    }
}


impl PveWebauthnConfig {
    /// Serialise this PveWebauthnConfig into Proxmox's CLI-style shorthand
    /// string (`key=value,…`). The property marked `x-pve-default-key`
    /// is emitted positionally without a `key=` prefix; aliases collapse
    /// multiple property names to the same wire key.
    ///
    /// Example: `PveWebauthnConfig `
    /// → `"virtio,bridge=vmbr0"`
    pub fn to_shorthand(&self) -> String {
        let mut parts: Vec<String> = Vec::new();
        
        
        
        if let Some(ref v) = self.allow_subdomains {
            parts.push(format!("allow-subdomains={}", v));
        }
        
        
        if let Some(ref v) = self.id {
            parts.push(format!("id={}", v));
        }
        
        
        if let Some(ref v) = self.origin {
            parts.push(format!("origin={}", v));
        }
        
        
        if let Some(ref v) = self.rp {
            parts.push(format!("rp={}", v));
        }
        
        parts.join(",")
    }
}

