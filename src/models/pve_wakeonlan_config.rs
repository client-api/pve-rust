/*
 * Proxmox Virtual Environment API
 *
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 9.x
 * 
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PveWakeonlanConfig {

    /// Bind to this interface when sending wake on LAN packet
    #[serde(rename = "bind-interface", skip_serializing_if = "Option::is_none")]
    pub bind_interface: Option<String>,

    /// IPv4 broadcast address to use when sending wake on LAN packet
    #[serde(rename = "broadcast-address", skip_serializing_if = "Option::is_none")]
    pub broadcast_address: Option<String>,

    /// MAC address for wake on LAN
    #[serde(rename = "mac")]
    pub mac: String,


}

impl PveWakeonlanConfig {
    pub fn new(mac: String) -> PveWakeonlanConfig {
        PveWakeonlanConfig {
            
            bind_interface: None,
            
            broadcast_address: None,
            
            mac,
            
        }
    }
}


impl PveWakeonlanConfig {
    /// Serialise this PveWakeonlanConfig into Proxmox's CLI-style shorthand
    /// string (`key=value,…`). The property marked `x-pve-default-key`
    /// is emitted positionally without a `key=` prefix; aliases collapse
    /// multiple property names to the same wire key.
    ///
    /// Example: `PveWakeonlanConfig `
    /// → `"virtio,bridge=vmbr0"`
    pub fn to_shorthand(&self) -> String {
        let mut parts: Vec<String> = Vec::new();
        
        
        parts.push(format!("{}", self.mac));
        
        
        
        
        if let Some(ref v) = self.bind_interface {
            parts.push(format!("bind-interface={}", v));
        }
        
        
        if let Some(ref v) = self.broadcast_address {
            parts.push(format!("broadcast-address={}", v));
        }
        
        parts.join(",")
    }
}

